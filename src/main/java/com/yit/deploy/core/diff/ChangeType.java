package com.yit.deploy.core.diff;

public enum ChangeType {
    change, // value is changed
    insert, // insert an element to a specified index
    remove, // remove an element at specified index
    swap, // swap two elements in an array
}
